---
// Badges — the standard "no key required · CORS-open" chip pair shown top-right
// of most page heads. This is the default; pass custom chips into PageHead's
// `badges` slot to override.
---
<span class="chip chip-green">no key required</span>
<span class="chip chip-blue">CORS-open</span>
