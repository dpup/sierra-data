---
// GetChip — the "GET /api/v1/…" method pill shown next to section headings and
// endpoint references. Renders exactly the markup the pages used inline:
//   <span class="chip-get"><b>GET</b> /api/v1/events</span>
interface Props {
  path: string;
}
const { path } = Astro.props;
---
<span class="chip-get"><b>GET</b> {path}</span>
