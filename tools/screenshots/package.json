{
  "name": "sierra-site-screenshots",
  "private": true,
  "type": "module",
  "description": "Headless-browser screenshots + layout metrics for the site, for diagnosing spacing/layout issues. Requires Playwright + Chromium (baked into the container via moat.yaml hooks). Pin the playwright version to the same minor as moat.yaml's install hooks so the driver matches the pre-downloaded browser.",
  "devDependencies": {
    "playwright": "~1.49.0"
  },
  "scripts": {
    "shots": "node shots.mjs"
  }
}
