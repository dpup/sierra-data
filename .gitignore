# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Build artifacts
/bin/
/dist/
/build/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store

# Linux
*~

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs
*.log
logs/

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node_modules (if any JS tools are used)
node_modules/

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# direnv files
.envrc
.envrc.local

# Private journal files
.private-journal/

# Environment-specific config files
config.local.yaml
config.production.yaml
config.development.yaml

# NOTE: Generated protobuf Go code (*.pb.go, *_grpc.pb.go, *.pb.gw.go) is
# committed (not ignored) so the Docker build doesn't have to install protoc +
# plugins at build time. Regenerate with `make proto` after changing any .proto.

# Temporary files
*.tmp
*.temp
/tmp/

# Local development certificates
*.pem
*.key
*.crt
*.p12

# Database files
*.db
*.sqlite
*.sqlite3

# Local grid SQLite store (data/places/ seed geometry stays checked in)
data/*
!data/places/

# Cache directories
.cache/
*.cache

# Editor backups
*.bak
*.backup

# OS generated files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Project-specific ignores
/server
/test-*
/cmd/*/main

# API keys and secrets (even though they should be in env vars)
secrets.yaml
api-keys.yaml
# Local dev artifacts
server.log
server.pid
.claude/settings.local.json
