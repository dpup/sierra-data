# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Build artifacts
/bin/
/dist/
/build/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store

# Linux
*~

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs
*.log
logs/

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node_modules (if any JS tools are used).
# Both forms on purpose: `node_modules/` matches a real directory, but the site
# build makes web/node_modules a SYMLINK to an off-mount store (see the
# SITE_DEPS note in the Makefile), and a trailing-slash pattern does not match a
# symlink — so without the second line git would happily commit a link to an
# absolute path that exists on exactly one machine.
node_modules/
node_modules

# Astro build cache (web/). NOTE: the build OUTPUT (site/dist) is committed on
# purpose — like the generated *.pb.go — so the Docker `go build` stays Node-free.
web/.astro/

# ...except Astro's content-layer stubs, which the build emits inconsistently
# (empty `new Map()` modules, unreferenced by the served site) and which
# otherwise churn site/dist in and out of every commit. go:embed all:dist picks
# them up when present and is fine without them.
site/dist/content-assets.mjs
site/dist/content-modules.mjs

# Screenshot/layout-diagnosis artifacts (tools/screenshots) — regenerated.
tools/screenshots/out/

# Mocked-data screenshot review artifacts (web/screenshots) — regenerated.
web/screenshots/out/

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# direnv files
.envrc
.envrc.local

# Private journal files
.private-journal/

# Environment-specific config files
config.local.yaml
config.production.yaml
config.development.yaml

# NOTE: Generated protobuf Go code (*.pb.go, *_grpc.pb.go, *.pb.gw.go) is
# committed (not ignored) so the Docker build doesn't have to install protoc +
# plugins at build time. Regenerate with `make proto` after changing any .proto.

# Temporary files
*.tmp
*.temp
/tmp/

# Local development certificates
*.pem
*.key
*.crt
*.p12

# Database files
*.db
*.sqlite
*.sqlite3

# Local grid SQLite store (data/places/ seed geometry stays checked in)
data/*
!data/places/

# Cache directories
.cache/
*.cache

# Editor backups
*.bak
*.backup

# OS generated files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Project-specific ignores
/server
/test-*
/cmd/*/main

# API keys and secrets (even though they should be in env vars)
secrets.yaml
api-keys.yaml
# Local dev artifacts
server.log
server.pid
.claude/settings.local.json
